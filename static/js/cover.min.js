'use strict';
(function (a, b) {
    function c() {
        return !1
    }

    function d(n, o) {
        if (n.classList) n.classList.remove(o); else {
            var p = n.className.split(' ').filter(function (q) {
                return q != o
            });
            n.className = p.join(' ')
        }
    }

    var f = window.devicePixelRatio || 1, g = Math.round(100 * (1 / f)) / 100;
    window.scale = g;
    var h = a.createElement('meta');
    h.setAttribute('name', 'viewport'), h.setAttribute('content', 'width=device-width, initial-scale=' + g + ', maximum-scale=' + g + ', minimum-scale=' + g + ', user-scalable=no, viewport-fit=cover'), a.getElementsByTagName('head')[0].appendChild(h);
    var i = a.documentElement, j = 'orientationchange' in window ? 'orientationchange' : 'resize', k = function () {
        var n = i.clientWidth;
        if (n) {
            var o = 100 * (n / 750);
            i.style.fontSize = o + 'px';
            var p = ~~(1e4 * +window.getComputedStyle(document.getElementsByTagName('html')[0]).fontSize.replace('px', '')) / 1e4;
            Math.round(p) !== Math.round(o) && (document.getElementsByTagName('html')[0].style.fontSize = o * (o / p) + 'px')
        }
    };
    a.addEventListener && (b.addEventListener(j, k, !1), k(), a.addEventListener('DOMContentLoaded', function l() {
        var n = a.querySelector('div[scroll]'), o;
        return !n || void(n.addEventListener('touchstart', function (p) {
            o = p.touches[0].clientY, c(document.body, 'scrolling')
        }), n.addEventListener('touchmove', function (p) {
            var q = '11', r = this, s = p.touches[0].clientY;
            if (0 === r.scrollTop ? q = r.offsetHeight >= r.scrollHeight ? '00' : '01' : r.scrollTop + r.offsetHeight >= r.scrollHeight && (q = '10'), '11' != q) {
                var t = 0 < s - o ? '10' : '01';
                parseInt(q, 2) & parseInt(t, 2) || (p.preventDefault(), p.stopPropagation())
            }
        }), n.addEventListener('touchend', function () {
            d(document.body, 'scrolling')
        }))
    }, !1), a.addEventListener('DOMContentLoaded', function m() {
        (function () {
            var n = navigator.userAgent.toLowerCase(), o = null;
            (0 <= n.indexOf('iphone') || 0 <= n.indexOf('ipad')) && a.body.addEventListener('touchend', function (p) {
                var q = new Date().getTime();
                o = o || q + 1;
                var r = q - o;
                return 500 > r && 0 < r ? (p.preventDefault(), !1) : void(o = q)
            }, !1), a.body.addEventListener('touchstart', function () {
            })
        })()
    }, !1))
})(window.document, window);